# Legacy SQL Files

This directory contains deprecated SQL files that are **not** for use with the current application.

## schema-mysql-deprecated.sql

This was the original MySQL draft schema. It is **deprecated** and should not be used.

The active schema is `sql/schema-postgres.sql` (PostgreSQL/Supabase).
